/** @type {import('next').NextConfig} */
const nextConfig = {
	output: 'export',
	/* 	images: {
		remotePatterns: [
			{
				protocol: 'https',
				hostname: 'media.discordapp.net',
				port: '*',
				pathname: '*',
			},
		],
	}, */
}

module.exports = nextConfig

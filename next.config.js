/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = nextConfig

/* output: 'export', */
/* 	images: {
		remotePatterns: [
			{
				protocol: 'https',
				hostname: 'media.discordapp.net',
				port: '*',
				pathname: '*',
			},
		],
	}, */

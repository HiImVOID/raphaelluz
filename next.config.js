/** @type {import('next').NextConfig} */
const nextConfig = {
	output: 'export',
}

module.exports = nextConfig

/* 	images: {
		remotePatterns: [
			{
				protocol: 'https',
				hostname: 'media.discordapp.net',
				port: '*',
				pathname: '*',
			},
		],
	}, */
